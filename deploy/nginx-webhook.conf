server {
    listen 80;
    server_name webhookishkop.1wash.uz;

    # Redirect HTTP to HTTPS
    location / {
        return 301 https://$host$request_uri;
    }

    # Certbot challenge
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }
}

server {
    listen 443 ssl http2;
    server_name webhookishkop.1wash.uz;

    ssl_certificate /etc/letsencrypt/live/webhookishkop.1wash.uz/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/webhookishkop.1wash.uz/privkey.pem;

    # All requests go to FastAPI backend
    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 60s;
        proxy_connect_timeout 60s;
    }
}
